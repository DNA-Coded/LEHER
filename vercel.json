{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    {
      "src": "/earth/(.*)",
      "dest": "/earth/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
