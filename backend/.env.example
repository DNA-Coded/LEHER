# Leher Backend Configuration
# Copy this file to .env and adjust values as needed

# Root directory for storing datasets (outside Git repo)
LEHER_DATA_ROOT=/path/to/your/leher-data

# API Settings
API_HOST=0.0.0.0
API_PORT=8000